using System;
using System.Collections.Generic;
using System.Text;

namespace MathForGames
{
    class PlayerHud
    {/*
        private Player _player;
        private UIText _playersOnTeam;
        //private UIText _lives;

        public PlayerHud(Player player, UIText playersOnTeam)
        {
            _player = player;
            _playersOnTeam = playersOnTeam;
            //_lives = lives;
        }
        public override void Start()
        {
            base.Start();
            _playersOnTeam.Start();
           // _lives.Start();
        }

        public override void Update()
        {
            _playersOnTeam.Text = "Health : " + _player.PlayersOnTeam.ToString();
            //_lives.Text = "Lives: " + _player.Lives.ToString();

        }

        public override void Draw()
        {
            _playersOnTeam.Draw();
            //_lives.Draw();
        }*/
    }
}
